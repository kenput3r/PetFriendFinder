    <h2>{{name}}</h2>

    <img src="{{picture}}" />

    {{#each pets}}
        <p>{{name}}</p>
        <img src="{{picture}}" />
    {{/each}}

    {{#if canEdit}}
        <p>I can edit this</p>
    {{else}}
        <p>I can't edit this</p>
    {{/if}}